var instance_methods = [];
var class_methods = [];

var instance_methods = $("#method-list-section .link-list li a").map(function() {
  var method_name = $(this).text();

  if() return .replace(/^(#|::)/, '');
})
