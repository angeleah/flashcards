/ - if @unfinished_quizzes.empty?
/   Congrats! You have no unfinished quizzes.
/   %a{href: "/"} Take a quiz now
/ - else
/   %ul
/   - @unfinished_quizzes.each do |quiz|
/     %li
/       #{quiz.category}'s #{quiz.object_type} on #{local_display_date(quiz.created_at)}
/       %a{ href: quiz_session_path(quiz.id) } Finish the quiz.


/ .large-12.columns{role: "content"}
/   .row
/     .large-6.columns
/       %h3
/         Unfinished Quizzes
/       - if @unfinished_quizzes.empty?
/         Congrats! You have no unfinished quizzes.
/         %a{href: "/"} Take a quiz now
/       - else
/         %ul
/         - @unfinished_quizzes.each do |quiz|
/           %li
/             #{quiz.category}'s #{quiz.object_type} on #{local_display_date(quiz.created_at)}
/             %a{ href: quiz_session_path(quiz.id) } Finish the quiz.

/   %hr/

/ %aside.large-3.columns
/   %h5 Stats
/   - if @past_quizzes.empty?
/     %h7 You haven't quized yourself yet!
/     %a{href: "/"} Quiz yourself

/   - @past_quizzes.each do |quiz|
/     %ul.side-nav
/       %li
/         %a{href: "#{ stat_path(quiz.id)}"} #{ quiz.category }'s #{ quiz.object_type }
/         on #{ local_display_date(quiz.created_at) }
.row
  .large-4.columns
    %h3 Unfinished Quizzes
    - if @unfinished_quizzes.empty?
      %h7 Congrats! You have no unfinished quizzes.
      %a{href: "/"} Take a quiz now!
    - else
      - @unfinished_quizzes.each do |quiz|
        %ul.side-nav
          %li
            #{quiz.category}'s #{quiz.object_type} on #{local_display_date(quiz.created_at)}
            %a{ href: quiz_session_path(quiz.id) } Finish the quiz
